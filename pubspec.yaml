name: fire
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  
  flutter_bloc: ^9.1.1
  easy_locale: ^0.0.12
  easy_localization: ^3.0.8
  flutter_locoliazations:
    sdk: flutter
  firebase_core: ^4.2.1
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.1
  gap: ^3.0.1
  go_router: ^16.3.0
  lottie: 3.3.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
